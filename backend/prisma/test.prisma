datasource db {
  provider = "postgresql"
  url      = "postgresql://postgres:root@localhost:5432/Odoo?schema=public"
}

generator client {
  provider = "prisma-client-js"
}

model Test {
  id Int @id @default(autoincrement())
}
